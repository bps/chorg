[package]
name = "chorg"
version = "0.1.0"
edition = "2021"
description = "Structured org-mode document editor for humans and LLM agents"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
